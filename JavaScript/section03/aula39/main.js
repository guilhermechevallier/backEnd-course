//operadores de comparação;
/*
    > maior que;
    < menor que;
    >= maior ou igual;
    <= menor ou igual;
    == igualdade (varifica valor);
    === igualdade restrita (verifica valor e tipo) - recomendavel;
    != diferente (verifica valor);
    !== diferente restrita (verifica valor e tipo) - recomendavel;
*/

console.log(10 > 5); //verifica uma expressao e retorna true ou false;